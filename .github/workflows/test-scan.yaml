name: Checkov Scan

on:
  push:
    branches:
      - feature/README-changes
  pull_request:
    branches:
      - feature/README-changes

permissions:
  actions: read
  contents: read
  id-token: write
  security-events: write

jobs:
  checkov-scan:
    uses: UKHomeOffice/core-cloud-workflow-checkov-sast-scan/.github/workflows/checkov-scan-base.yaml@1.7.1
